<%= link_to project_category_article_path(project, category, article), class: "card mb-3", data: { turbo_frame: :project_content } do %>
  <div class="card-body">
    <%= article.title %>
  </div>
<% end %>
